<!DOCTYPE html>
<html>

<head>
    <title>Categories</title>
</head>

<body>
    <h1>Category List</h1>
    <ul>
        <% categories.forEach(category=> { %>
            <li>
                <%= category.name %>
            </li>
            <% }) %>
    </ul>

    <p>
        Total rows: <%=categories.length %>
    </p>
</body>

</html>